{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SopDocument",
  "description": "JSON Schema for SOP YAML source files (INTERFACES.md §1).",
  "type": "object",
  "required": ["sop_version", "name", "policy", "steps"],
  "additionalProperties": false,
  "properties": {
    "sop_version": {
      "type": "string",
      "description": "SOP schema version."
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Stable SOP identifier."
    },
    "policy": {
      "type": "string",
      "minLength": 1,
      "description": "Path to the policy file that sets the capability ceiling."
    },
    "steps": {
      "type": "array",
      "minItems": 1,
      "description": "Ordered list of steps in the SOP.",
      "items": {
        "type": "object",
        "required": ["id", "requires"],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "minLength": 1,
            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
            "description": "Stable step identifier."
          },
          "description": {
            "type": "string",
            "minLength": 1,
            "description": "Human-readable description of the step."
          },
          "requires": {
            "type": "object",
            "required": ["capabilities"],
            "additionalProperties": false,
            "properties": {
              "capabilities": {
                "type": "array",
                "minItems": 1,
                "items": {
                  "type": "string",
                  "minLength": 1
                },
                "description": "Capability strings required by this step."
              }
            }
          },
          "on_success": {
            "type": "string",
            "minLength": 1,
            "description": "Target step id on success."
          },
          "on_failure": {
            "type": "string",
            "minLength": 1,
            "description": "Target step id on failure."
          },
          "terminal": {
            "type": "boolean",
            "default": false,
            "description": "Whether this is a terminal (end) step."
          }
        }
      }
    }
  }
}
